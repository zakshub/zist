import{MockFacebookContentGenerator,validateVariants}from'@zak/facebook';import{FacebookPagePostRepository,migrate,openDatabase}from'@zak/db';
const db=openDatabase();try{migrate(db);const repository=new FacebookPagePostRepository(db);const article=repository.pendingArticle();if(!article){console.log(JSON.stringify({created:false,reason:'No article is waiting for Facebook content.'},null,2));process.exit(0)}const variants=new MockFacebookContentGenerator().generate(article);const issues=validateVariants(variants);if(issues.length)throw new Error(`Facebook content validation failed: ${issues.join('; ')}`);const saved=repository.create({articleId:article.articleId,bloggerPublicationId:article.bloggerPublicationId,variants,generationVersion:'facebook-content-v1'});console.log(JSON.stringify({created:true,id:saved.id,articleId:saved.articleId,status:saved.status,variants:Object.keys(saved.variants),generationVersion:saved.generationVersion},null,2))}finally{db.close()}
