import{normalizeMetrics}from'@zak/analytics';import{AnalyticsRepository,migrate,openDatabase}from'@zak/db';
const db=openDatabase();try{migrate(db);const repository=new AnalyticsRepository(db);const capturedAt=process.env.ANALYTICS_CAPTURED_AT||'2026-08-13T00:00:00.000Z';let created=0;repository.postIds().forEach((postId,index)=>{const multiplier=index+1;const metrics=normalizeMetrics({impressions:500*multiplier,reach:400*multiplier,reactions:18*multiplier,comments:5*multiplier,shares:3*multiplier,linkClicks:20*multiplier});if(repository.create(postId,capturedAt,'MOCK',metrics))created+=1});console.log(JSON.stringify({provider:'MOCK',capturedAt,created,totalSnapshots:repository.count(),topicSummary:repository.topicSummary()},null,2))}finally{db.close()}
