import{recommendSchedule}from'@zak/scheduling';import{migrate,openDatabase,ScheduleRepository}from'@zak/db';
const db=openDatabase();try{migrate(db);const repository=new ScheduleRepository(db);const now=process.env.SCHEDULER_NOW||new Date().toISOString();let created=0;let skipped=0;for(const candidate of repository.candidates()){const recommendation=recommendSchedule(now,candidate.opportunityScore,repository.existingTimes());if(!recommendation){skipped+=1;continue}repository.create({facebookPagePostId:candidate.id,opportunityScore:candidate.opportunityScore,...recommendation});created+=1}console.log(JSON.stringify({now,created,skipped,recommendations:repository.list()},null,2))}finally{db.close()}
